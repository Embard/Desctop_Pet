name: Build Windows EXE

on:
  workflow_dispatch:
  push:
    branches:
      - main
      - master
    paths:
      - "desktop_pet/**"
      - ".github/workflows/build-windows.yml"

jobs:
  build:
    name: Build DesktopPet.exe
    runs-on: windows-latest

    steps:
      - name: Check out repository
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.12"

      - name: Install dependencies
        working-directory: desktop_pet
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt

      - name: Build sprite sheets
        working-directory: desktop_pet
        run: |
          if (!(Test-Path assets\reference_face.png) -and !(Test-Path assets\reference_pose.png)) {
            Write-Error "Missing assets/reference_face.png or assets/reference_pose.png in repository"
          }
          python sprite_builder.py
          if (!(Test-Path assets\animations.json)) {
            Write-Error "Sprite build did not produce assets/animations.json"
          }

      - name: Build executable
        working-directory: desktop_pet
        run: |
          pyinstaller --noconfirm --clean --windowed --onefile --name DesktopPet --add-data "assets;assets" main.py

      - name: Upload executable
        uses: actions/upload-artifact@v4
        with:
          name: DesktopPet-windows
          path: desktop_pet/dist/DesktopPet.exe
          if-no-files-found: error
